{
  "name": "wadl-proxy",
  "version": "1.0.0",
  "description": "Simple WADL proxy for Vitalsjs to avoid CORS when fetching NASA WADL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
